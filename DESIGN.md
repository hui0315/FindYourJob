# FindYourJob 設計理念

## 核心哲學：資料閉環，不依賴外部知識

本系統的所有智慧判斷，都來自使用者自己輸入的資料。不仰賴市場行情資料庫、不需要爬蟲抓人力銀行、不依賴大型雲端 LLM 的世界知識。

這不是因為做不到，而是刻意的設計選擇。

---

## 為什麼這樣設計

### 問題：求職者的真實痛點

求職者從各種管道（104、LinkedIn、朋友轉發、社群貼文）蒐集到的職缺資訊，格式混亂、散落各處。他們需要的不是另一個職缺搜尋引擎，而是一個**整理工具**——把已經找到的東西理清楚。

### 傳統做法的問題

大多數職缺平台試圖做「全知系統」：
- 需要爬取完整的市場數據才能評分
- 需要大量標註資料訓練推薦模型
- 需要持續維護產業/職位/薪資資料庫
- 需要使用者授權存取個人履歷

這些都意味著**高維護成本、外部依賴、隱私風險**。

### 我們的做法：讓資料自己說話

> 評分不需要知道「市場行情」，只需要知道「你蒐集的這 20 筆職缺裡，彼此之間的相對關係」。

---

## 三個關鍵設計決策

### 1. 輕量本地 LLM 只做萃取，不做評價

我們使用 Ollama + Qwen2.5-3B（約 2GB），任務很單純：

```
輸入：一段混亂的職缺文字
輸出：固定 schema 的 JSON
```

LLM 不需要「懂」這份工作好不好，它只需要識別「哪段文字是薪資、哪段是技能需求」。這是 3B 參數模型就能勝任的文字萃取任務，不需要 GPT-4 等級的推理能力。

當 Ollama 不可用時，系統自動退回 regex 解析，功能不中斷。

### 2. 評分基於相對排名，不基於絕對標準

系統不會告訴你「月薪 60K 在市場上算高還是低」——因為我們沒有市場數據，硬算就是在騙人。

但系統可以告訴你：

- 「這筆薪資在你蒐集的 15 筆裡排第 3」
- 「這筆的技能匹配度是 75%，另一筆只有 40%」

**相對比較的資訊量，對決策來說完全足夠。** 求職者要的是「這幾份工作裡哪個最適合我」，不是「這份工作在全台灣排第幾」。

### 3. 技能池從職缺中生長，不是預設清單

傳統做法：維護一個龐大的技能資料庫（幾千個技能標籤），讓使用者從中挑選。

我們的做法：

```
使用者加入的職缺 → 自動萃取技能關鍵字 → 彙整成技能池 → 使用者勾選：已會 / 可補強 / 未選
```

好處：
- **詞彙完全對齊**——技能池裡的詞就是職缺裡的詞，不會有「我寫 React，職缺寫 React.js」的匹配問題
- **零維護成本**——不需要有人去更新技能資料庫
- **自然成長**——使用者加越多職缺，技能池越完整
- **高度相關**——池裡只會出現使用者關心的領域的技能，不會出現無關的雜訊

---

## 不符合條件的警告機制

使用者先設定自己的硬性條件（年資、學歷、最低薪資），系統在每次新增職缺時自動比對：

- 符合 → 正常顯示
- 不符合 → 紅框 + 具體原因（如「要求 5 年經驗，你有 3 年」）

**關鍵設計：不符合的職缺不會被隱藏或排除。**

因為：
- LLM 萃取可能有誤，使用者需要自己判斷
- 使用者可能有其他考量（例如願意為好公司降薪）
- 警告的目的是「提醒」，不是「替你決定」

---

## 技術棧選擇的一致性

| 選擇 | 理由 |
|------|------|
| SQLite | 單檔案、零配置，像 Excel 一樣直覺 |
| FastAPI | Python 生態最輕量的 API 框架 |
| Ollama | 本地推理、一行安裝、不需 API key |
| Qwen2.5-3B | 同體量中文最強，CPU 可跑 |
| React + Tailwind | 互動性與開發速度的平衡 |

所有元件都可以在一台普通筆電上離線運行。不需要雲端服務、不需要付費 API、不需要資料庫伺服器。

---

## 結構化待遇資料

### 為什麼需要結構化

使用者從 104、1111 等人力銀行複製職缺資訊時，資方通常會條列以下結構化資料：

- **薪資結構**：不只是「月薪 XX」，還包含保障年薪月數、年終獎金月數
- **福利制度**：人力銀行將福利分為獎金、保險、休假、補助、制度等六大類
- **工作條件**：休假制度、語文條件等

如果只存成一個 `benefits` 純文字欄位，這些資訊就失去了比較的價值。

### 結構化方式

參考 104 人力銀行的分類標準，將福利拆成六類：

| 分類 | 欄位 key | 常見項目 |
|------|----------|---------|
| 獎金類 | `bonus` | 年終獎金、三節獎金、績效獎金、全勤獎金、員工分紅 |
| 保險類 | `insurance` | 團體保險、意外險 |
| 休假類 | `leave` | 特休優於勞基法、彈性假、有薪病假 |
| 補助類 | `subsidy` | 旅遊補助、結婚補助、生育補助、進修補助、交通補助 |
| 制度類 | `system` | 教育訓練、員工持股、彈性上下班 |
| 其他 | `other` | 員工旅遊、免費零食、健身房、尾牙 |

薪資方面新增 `salary_guaranteed_months`（保障年薪月數），直接對應人力銀行上「保障14個月」這類常見描述。

### 設計原則不變

結構化是為了讓使用者自己的資料更好比較，不是為了接外部市場數據。仍然是**使用者自行輸入 → 系統結構化萃取 → 相對比較**的閉環。

---

## 公司與職缺分離

### 為什麼要分開

同一間公司的多個職缺，通常共享相同的福利制度、聯絡方式。如果全部塞在 `jobs` 表裡：

- **重複儲存**：5 個 Google 職缺 = 5 份一樣的福利資料
- **不一致**：每次 LLM 萃取可能略有差異
- **沒地方放公司層級資訊**：HR 聯絡方式、公司地址、公司網站

### 架構

```
companies (公司層級)
├── name           公司名稱
├── benefits       通用福利制度
├── contact_*      聯絡人/電話/Email
├── address        公司地址
└── website        公司網站

jobs (職缺層級)
├── company_id     → FK to companies
├── title, salary, skills, ...  職缺條件
├── benefits       職缺專屬額外福利（補充公司福利）
└── ...
```

### 福利歸屬策略：公司為主，職缺可補充

- **新增職缺時**：若公司不存在，建立公司並將福利移至公司層級。若公司已存在且有福利，職缺的福利自動成為「額外補充」。
- **顯示時**：公司福利 + 職缺額外福利合併呈現，額外部分有標記。
- **聯絡資訊**：儲存在公司層級，非破壞性更新（只填補空欄位）。

### 公司名稱匹配

目前使用精確字串比對。LLM 萃取出的公司名稱可能不一致（「台積電」vs「台灣積體電路」），使用者可在公司管理介面手動修正。

### 設計原則不變

分離是為了消除冗餘，讓使用者更好地管理公司資訊。仍然是**使用者自行輸入 → 系統結構化 → 相對比較**的閉環。

---

## 一句話總結

> **不做全知系統，做最好的整理工具。所有判斷都從使用者自己的資料中來，不假裝知道我們不知道的事。**
